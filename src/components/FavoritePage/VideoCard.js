import React from "react";
import { Box } from "@mui/material";

const VideoCard = () => {
  return (
    <Box
      sx={{
        background: "#FFFFFF",
        borderRadius: "10px",
        border: "1px solid #EAEAEA",
      }}
    >
      Content
    </Box>
  );
};

export default VideoCard;
